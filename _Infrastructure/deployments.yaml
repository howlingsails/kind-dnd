apiVersion: apps/v1
kind: Deployment
metadata:
  name:  cap10bills-fantasy-map
  namespace: default
  labels:
    app:  cap10bills-fantasy-map
spec:
  selector:
    matchLabels:
      app: cap10bills-fantasy-map
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app:  cap10bills-fantasy-map
    spec:
      # initContainers:
        # Init containers are exactly like regular containers, except:
          # - Init containers always run to completion.
          # - Each init container must complete successfully before the next one starts.
      containers:
      - name:  cap10bills-fantasy-map
        image:  "kinddnd:latest"
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
          limits:
            cpu: 100m
            memory: 100Mi
        livenessProbe:
          tcpSocket:
            port: 8090
          initialDelaySeconds: 5
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /_status/healthz
            port: 8090
          initialDelaySeconds: 5
          timeoutSeconds: 2
          successThreshold: 1
          failureThreshold: 3
          periodSeconds: 10
        env:
        - name: DB_HOST # What should this name be?
          valueFrom:
            configMapKeyRef:
              name: cap10bills-fantasy-map
              key: DB_HOST
        ports:
        - containerPort:  8090
          name:  cap10bills-fantasy-map
        volumeMounts:
        - name: localtime
          mountPath: /etc/localtime
      volumes:
        - name: localtime
          hostPath:
            path: /usr/share/zoneinfo/Asia/Shanghai
      restartPolicy: Always